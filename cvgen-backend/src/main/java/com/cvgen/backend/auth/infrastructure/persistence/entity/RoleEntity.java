package com.cvgen.backend.auth.infrastructure.persistence.entity;

/**
 * Rôles applicatifs de CVGen.
 * Stocké en base sous forme de chaîne (EnumType.STRING).
 */
public enum RoleEntity {
    ROLE_USER,
    ROLE_ADMIN
}
